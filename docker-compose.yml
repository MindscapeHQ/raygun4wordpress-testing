version: "3.3"

services:
   submodule-updater:
     image: alpine/git:latest
     env_file:
       - settings.env
     profiles: ["update"]
     volumes:
       - ./:/repository
     entrypoint: ash /repository/scripts/update_submodule.sh
     environment:
       - UPDATE=true

   cleaner:
     image: alpine/git:latest
     env_file:
       - settings.env
     profiles: ["clean"]
     volumes:
       - ./:/repository
     entrypoint: ash /repository/scripts/update_submodule.sh
     environment:
       # Use update_submodule.sh for cleaning and removing API key only
       - UPDATE=false

   runner:
     depends_on:
       - db
     build:
       dockerfile: Dockerfile
     env_file:
       - settings.env
     profiles: ["test"]
     volumes:
       - ./raygun-testing:/test_plugin
       - ./scripts/:/scripts/
     entrypoint: ash /scripts/run_tests.sh
     user: xfs
     environment:
       WORDPRESS_DB_HOST: db:3306
       WORDPRESS_DB_USER: wordpress
       WORDPRESS_DB_PASSWORD: wordpress
       WORDPRESS_DB_NAME: wordpress
       WORDPRESS_TABLE_PREFIX: "wp_"

   db:
     image: mysql:5.7
     profiles: ["test"]
     volumes:
       - db_data:/var/lib/mysql
     restart: always
     environment:
       MYSQL_ROOT_PASSWORD: wordpress
       MYSQL_DATABASE: wordpress
       MYSQL_USER: wordpress
       MYSQL_PASSWORD: wordpress

volumes:
    db_data: